
CMAKE_MINIMUM_REQUIRED(VERSION 2.6 FATAL_ERROR)
CMAKE_POLICY(VERSION 2.6)
FIND_PACKAGE(Torch REQUIRED)
FIND_PACKAGE(Boost REQUIRED)

SET(luasrc init.lua File.lua)

ADD_TORCH_PACKAGE(csv "" "${luasrc}" "Image Processing")
